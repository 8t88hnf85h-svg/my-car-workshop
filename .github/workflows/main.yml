- name: Upload to TestFlight
        run: |
          xcrun altool --upload-app --type ios --file path_to_your_app.ipa --apiKey ${{ secrets.APP_STORE_CONNECT_KEY_IDENTIFIER }} --apiIssuer ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
